/* @flow */

import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    flexDirection: 'row',
  },
  imgLogo: {
    width: 60,
    height: 60,
  },
  value: {
    width: 40,
    textAlign: 'center',
  },
})

export default styles
